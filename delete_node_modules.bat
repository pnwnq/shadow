@echo off
echo Attempting to delete the node_modules directory...
rd /s /q "node_modules"
if exist "node_modules" (
    echo Failed to delete node_modules completely. It might be in use or require administrator privileges.
) else (
    echo Successfully deleted node_modules.
)
echo You can now close this window or run pnpm install in your terminal.
pause 